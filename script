# Parse raw midi data to np array
python3 parse_midi 

# trian the model on the np array data
python3 model_2

# use the model to generate new music pieces

# Parse music pieces back to midi files
python3 create_midi
